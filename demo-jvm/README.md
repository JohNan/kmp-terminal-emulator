# KMP Terminal - JVM Desktop Demo

This folder contains a desktop application built with Compose Multiplatform running the terminal emulator natively on JVM (Windows, macOS, Linux).

## Running the Demo

To launch the desktop application, run:
```bash
mise run run-jvm
```
Or manually via Gradle:
```bash
./gradlew :demo-jvm:run
```
